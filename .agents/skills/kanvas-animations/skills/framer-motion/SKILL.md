> **Not applicable to this project.** Kanvas site uses vanilla JS + GSAP, not React.
> Framer Motion requires React as a peer dependency.
>
> Use the **gsap** or **scroll-animations** skills instead.
> For hover transitions, see the **css-animations** skill.
